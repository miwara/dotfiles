# -*- mode: snippet -*-
# name: def_is_prime
# key: defisprime
# --
import math

def is_prime(n: int) -> bool:
    if n == 1: return False

    for k in range(2, int(math.sqrt(n)) + 1):
        if n % k == 0:
            return False

    return True
